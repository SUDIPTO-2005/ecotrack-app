"""Offsets app configuration."""
from django.apps import AppConfig


class OffsetsConfig(AppConfig):
    """Offsets app configuration."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.offsets"
    verbose_name = "Carbon Offset Marketplace (Informational)"
